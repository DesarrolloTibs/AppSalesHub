import { Injector } from '@angular/core';
import { DinamycPipePipe } from './dinamyc-pipe.pipe';

describe('DinamycPipePipe', () => {
  it('create an instance', () => {
   // const pipe = new DinamycPipePipe(Injector)
    //expect(pipe).toBeTruthy();
  });
});
